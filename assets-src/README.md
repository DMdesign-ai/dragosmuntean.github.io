# Original image sources

Kept out of `public/` so they are not deployed. The site serves the WebP
conversions in `public/images/`, which are ~84% smaller (1913 KB -> 297 KB
across the set) with no visible loss on these screenshots.

Re-run the conversion with sharp (already a dependency via Astro) if you
ever need to regenerate at a different quality.
